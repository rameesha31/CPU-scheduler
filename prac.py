 # # Create a tab view
            # tab_view = ctk.CTkTabview(gif_Label, width=1060, height=590)
            # tab_view.pack(pady=5, padx=5, fill="both", expand=True)

            # # Add named tabs
            # tabs = []
            # tab_names = ["FCFS", "SJF", "RR", "PS", "MQS"]
            # for name in tab_names:
            #     tab = tab_view.add(name)
            #     tabs.append(tab)
            

            # # Create a frame inside the first tab to hold the GIF
            # gif_frame = tk.Frame(tabs[0])
            # gif_frame.pack(padx=10, pady=10, fill="both", expand=True)

            # # Create the label for the GIF inside the frame
            # gif_Label = tk.Label(gif_frame, image=frames[0])
            # gif_Label.pack(fill="both", expand=True)

            # # Start the animation
            # animation()